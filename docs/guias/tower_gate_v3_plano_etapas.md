# Tower Gate v3 — plano de execucao por etapas (MIGRADO)

Este documento foi migrado em 2026-08-19 para
[`docs/specs-arena-180/`](../specs-arena-180/README.md), onde cada unidade de
trabalho e um arquivo e o **estado de todas elas** fica num quadro so.

Ele existia sem nenhuma marca de status, entao nao dava para saber o que ja tinha
sido feito — que e exatamente o problema que o diretorio novo resolve.

Onde cada parte foi parar:

| Secao original | Destino |
| --- | --- |
| §1 decisoes D1–D8 | [`decisoes.md`](../specs-arena-180/decisoes.md), renomeadas `DJ-1..DJ-8` para nao colidir com as `D1..D3` da spec 08 (agora `DR-*`) |
| §2 invariantes | secao "Invariantes" do [quadro](../specs-arena-180/README.md). Dois deles morreram com o pipeline de medicao de piso, e o quadro diz por que |
| §3 Etapas 1–11 | `JG-01`..`JG-11`. As unidades abertas tem arquivo proprio; as fechadas viraram linha no quadro |
| §4 ondas | secao "Ondas" do quadro, com a ordem **atualizada**: a RA-F5 subiu para primeiro |
| §5 o que o plano nao resolve | secao "Limites do stack" do quadro |
| §0 o que a v3 derruba | superado pelo quadro, que mostra o estado real de cada arquivo |

Este arquivo permanece so para nao quebrar os links que apontam para ele.
